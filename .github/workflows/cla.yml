name: CLA

# Every contributor signs the CLA (CLA.md) once, by commenting on their first
# pull request. Signatures are kept on the `cla-signatures` branch of this
# repository. The check blocks merging until the author has signed.

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA assistant
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path-to-signatures: signatures/cla.json
          path-to-document: https://github.com/${{ github.repository }}/blob/main/CLA.md
          branch: cla-signatures
          allowlist: varindersingh,dependabot[bot]
          custom-notsigned-prcomment: 'Thank you for the contribution. Before it can be reviewed, please read [CLA.md](https://github.com/${{ github.repository }}/blob/main/CLA.md) and sign it by replying with the sentence below.'
